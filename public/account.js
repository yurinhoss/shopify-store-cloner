(()=>{const nativeFetch=window.fetch.bind(window);window.Account={user:null,csrfToken:null};Account.ready=nativeFetch('/api/account/me').then(async r=>{if(!r.ok){location.replace('/login');throw Error('Entre novamente.');}Object.assign(Account,await r.json());return Account;});window.fetch=async(input,init={})=>{const url=new URL(typeof input==='string'?input:input.url,location.href);if(url.origin===location.origin&&url.pathname.startsWith('/api/')){await Account.ready;const headers=new Headers(init.headers|| (input instanceof Request?input.headers:undefined));headers.set('X-CSRF-Token',Account.csrfToken);init={...init,headers};}const r=await nativeFetch(input,init);if(r.status===401&&url.origin===location.origin)location.replace('/login');return r;};Account.logout=async()=>{await fetch('/api/account/logout',{method:'POST'});location.replace('/login');};})();
